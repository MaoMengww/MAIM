package consts

// Service.go is intentionally sparse — all service names, ports, metrics
// and config defaults were unused after the migration cleanup.
// They've been removed. If a future service needs them, re-add here.
