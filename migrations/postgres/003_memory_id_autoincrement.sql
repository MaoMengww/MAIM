-- Convert bot.bot_memories.id to auto-increment for existing deployments.
-- For PostgreSQL 12+, use identity column to align with BIGSERIAL in 000_schema.sql.
ALTER TABLE bot.bot_memories
    ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY;
