input {
  gelf {
    port => 12201
  }
}

filter {
  if [level] == "stat" {
    drop {}
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "aim-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
